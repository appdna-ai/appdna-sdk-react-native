import type { PaywallContext } from '@appdna-ai/react-native-sdk';
const context: PaywallContext = {
  placement: 'settings',
  experiment: 'paywall_redesign',
  variant: 'variant_a',
  customData: { feature_name: 'advanced_analytics' },
};

export {};
