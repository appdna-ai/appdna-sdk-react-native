# SPEC-070-B P0 — the wrapper adds no reflection of its own. The core SDK ships its own consumer
# rules; this file exists so `consumerProguardFiles` resolves, and as the place to put a rule if the
# TurboModule adapter ever needs one.
